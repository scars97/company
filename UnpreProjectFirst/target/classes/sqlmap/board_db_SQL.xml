<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<insert id="boardinsert" parameterType="hashMap"
		useGeneratedKeys="true" keyProperty="b_num">  
    <![CDATA[
    insert into board
    (b_title, nickname, b_content,category, user_id)
    values
    (#{b_title}, #{nickname}, #{b_content}, #{category}, #{user_id})
    ]]>
	</insert>

	<!-- 내가 쓴 글 찾아오기 -->
	<select id="mypageboard" parameterType="hashMap"
		resultType="hashMap">  
	<![CDATA[  
	select 
	b_num, b_title, nickname, cdate, b_content, category
	from board
	where user_id = #{user_id}
	]]>
	</select>

</mapper>